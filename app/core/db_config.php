<?php

/**
 * Database credentials are saved in their own file so we can add them to .GITIGNORE
 */
class DB_Config {
	
	protected $servername = 'localhost';
	protected $username = 'root';
	protected $password = 'root';
	protected $dbname = 'mvc';

}
	
	
	